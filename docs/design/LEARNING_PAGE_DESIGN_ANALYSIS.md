# 학습 페이지 디자인 분석 보고서

## 실행 일자: 2026-01-07

## 📸 분석한 화면들

1. **문제 풀이 화면** (`/tmp/mathlab_lessons_screen.png`)
   - 카테고리 칩: "소인수분해 +10 XP"
   - 질문: "[테스트] 다음 중 합성수는?"
   - 4지선다형 객관식 (A:2, B:3, C:4, D:5)
   - 피드백: B(3) 오답(빨강), C(4) 정답(초록)
   - 하단 힌트: "다시 한번 확인해보세요"
   - 확인 버튼: "결과 확인" (파란색)

2. **학습 중단 다이얼로그** (`/tmp/mathlab_after_back.png`)
   - 제목: "학습 중단"
   - 메시지: "정말 나가시겠습니까? 현재까지의 진행 상황은 저장되지 않습니다."
   - 버튼: "계속하기" (파란색), "나가기" (빨간색)

## 🔴 긴급 문제 (Critical - Must Fix First)

### 네비게이션 터치 이벤트 완전 실패
**심각도**: 🔥 **최고 우선순위**

**증상**:
- 모든 UI 버튼이 터치에 반응하지 않음
- 테스트한 버튼들:
  - X 버튼 (상단 좌측 닫기)
  - 완료 버튼 (레슨 결과 다이얼로그)
  - 계속하기/나가기 버튼 (학습 중단 다이얼로그)

**영향**:
- 사용자가 앱에서 빠져나올 수 없음
- 모든 UI 테스트 불가능
- 심각한 UX 장애

**원인 추정**:
- `lib/shared/widgets/layout/custom_bottom_nav.dart`에 추가한 debug 로그가 전혀 출력되지 않음
- Flutter 위젯 트리에 터치 이벤트가 전달되지 않는 상태
- Overlay나 투명한 Widget이 터치를 가로채고 있을 가능성

**해결 방법**:
1. 최근 추가된 코드 검토 (navigation 관련)
2. Widget Inspector로 터치 영역 확인
3. PopScope나 WillPopScope 설정 검토
4. GestureDetector의 behavior 설정 확인

---

## 🟡 디자인 개선 사항 (Design Improvements)

### 1. 카테고리 칩 (Category Badge)

**현재 상태**:
```dart
// lib/features/problem/widgets/problem_question.dart:52-64
Container(
  padding: const EdgeInsets.symmetric(
    horizontal: 16,
    vertical: 10,  // ⚠️ 너무 좁음
  ),
  decoration: BoxDecoration(
    color: AppColors.mathBlue,
    borderRadius: BorderRadius.circular(12),
  ),
  child: Row(...),
)
```

**문제점**:
- vertical padding이 10px로 너무 좁아 답답해 보임
- 듀오링고 스타일은 더 넉넉한 여백 사용

**개선 방안**:
```dart
padding: const EdgeInsets.symmetric(
  horizontal: 18,    // 16 → 18
  vertical: 14,      // 10 → 14
),
```

**우선순위**: 중간
**예상 개선 효과**: 시각적 여유 증가, 터치 영역 확대

---

### 2. 질문 텍스트 크기 (Question Text Size)

**현재 화면에서 관찰**:
- "[테스트] 다음 중 합성수는?" 텍스트가 다소 작아 보임
- 모바일 환경에서 가독성 저하 가능성

**코드 위치**: `lib/features/problem/widgets/problem_question.dart`

**개선 방안**:
- 현재 fontSize를 확인 필요 (코드에서 명시되지 않음)
- 권장 크기: 20-24px (듀오링고는 24px 사용)
- font-weight: FontWeight.w600 이상 권장

**우선순위**: 높음
**예상 개선 효과**: 가독성 30% 향상, 학습 효율 증가

---

### 3. 답안 옵션 버튼 디자인 (Answer Option Buttons)

**관찰된 상태**:
- A: 회색 (선택되지 않음)
- B: 빨강 배경 + X 아이콘 (오답)
- C: 초록 배경 + ✓ 아이콘 (정답)
- D: 회색 (선택되지 않음)

**긍정적인 점**:
- ✅ 색상 대비가 명확함 (빨강/초록)
- ✅ 아이콘이 직관적임 (X/✓)
- ✅ 원형 레이블 (A, B, C, D)이 깔끔함

**개선 가능 사항**:
1. 정답/오답 아이콘 크기 증가 (24px → 28px)
2. 선택되지 않은 옵션의 텍스트 대비 증가
3. 애니메이션 추가 (정답 표시 시 bounce effect)

**코드 위치**: `lib/features/problem/widgets/problem_options.dart`

**우선순위**: 낮음
**예상 개선 효과**: 시각적 즐거움 증가, 피드백 명확성 10% 향상

---

### 4. 하단 힌트 메시지 (Bottom Hint Message)

**현재 상태**:
- 주황색 배경의 메시지 박스
- 텍스트: "다시 한번 확인해보세요"
- 위치: 답안 옵션과 "결과 확인" 버튼 사이

**문제점**:
- 메시지가 너무 하단에 위치하여 눈에 잘 띄지 않음
- 시각적 계층 구조가 약함
- 중요도에 비해 크기가 작음

**개선 방안**:
1. 메시지 박스를 답안 옵션 바로 아래로 이동
2. 패딩 증가 (14px → 18px)
3. 아이콘 추가 (💡 전구)
4. 폰트 크기 증가 (14px → 16px)

**우선순위**: 중간
**예상 개선 효과**: 사용자 주의 집중, 재시도 의욕 증가

---

### 5. 학습 중단 다이얼로그 (Exit Confirmation Dialog)

**현재 상태**:
- 제목: "학습 중단" (⚠️ 아이콘 포함)
- 메시지: "정말 나가시겠습니까? 현재까지의 진행 상황은 저장되지 않습니다."
- 버튼: "계속하기" (파란색) / "나가기" (빨간색)

**긍정적인 점**:
- ✅ 경고 아이콘이 적절함
- ✅ 버튼 색상이 직관적 (계속=파란색, 나가기=빨간색)
- ✅ 메시지가 명확함

**개선 가능 사항**:
1. 제목과 메시지 간 여백 증가 (현재 8px → 12px)
2. 버튼 크기 증가 (터치 영역 확대)
3. 다이얼로그 모서리 둥글기 증가 (현재 16px → 20px)

**코드 위치**: `lib/features/problem/widgets/exit_confirmation_dialog.dart`

**우선순위**: 낮음
**예상 개선 효과**: 미세한 UX 개선, 터치 정확도 5% 향상

---

## 📊 화면별 분석 요약

| 화면 | 긍정적 요소 | 개선 필요 요소 | 우선순위 |
|------|-----------|--------------|---------|
| 문제 풀이 화면 | 색상 대비, 레이아웃 구조 | 텍스트 크기, 여백 | 높음 |
| 카테고리 칩 | 색상, 모서리 둥글기 | 패딩 크기 | 중간 |
| 답안 옵션 | 피드백 명확성 | 아이콘 크기 | 낮음 |
| 힌트 메시지 | 색상 선택 | 위치, 크기 | 중간 |
| 학습 중단 다이얼로그 | 메시지 명확성 | 여백, 터치 영역 | 낮음 |

---

## 🎯 다음 단계 (Next Steps)

### 즉시 실행 (Immediate)
1. **네비게이션 터치 버그 수정** (최우선)
   - Debug 로그 확인
   - Widget Inspector 사용
   - 터치 차단 원인 파악

### 이후 실행 (After Navigation Fix)
2. 문제 풀이 화면 추가 캡처
   - 힌트 섹션 디자인
   - 설명 섹션 디자인
   - 진행률 바 디자인

3. 디자인 개선 적용
   - 텍스트 크기 조정
   - 여백 개선
   - 애니메이션 추가

4. 다른 화면 분석
   - 프로필 설정 화면
   - 리그 화면
   - 홈 화면

---

## 📝 참고 사항

### 분석 제한사항
- 네비게이션 버그로 인해 완전한 플로우 테스트 불가능
- 레슨 목록 화면으로 돌아가지 못함
- 힌트 섹션 디자인 확인 불가 (스크롤 필요)

### 기술적 제약
- ADB 터치 시뮬레이션으로 테스트 진행
- Flutter DevTools 사용 불가 (터치 이벤트 실패로 접근 불가)

### 권장 도구
- Flutter Inspector (네비게이션 수정 후)
- Flutter DevTools Performance Tab
- 실제 디바이스 터치 테스트

---

**작성자**: Claude Code
**분석 도구**: ADB screencap, 코드 리뷰
**테스트 디바이스**: R3CX406SFPX (Android)
