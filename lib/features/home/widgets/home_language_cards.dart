import 'package:flutter/material.dart';

class HomeLanguageCards extends StatelessWidget {
  const HomeLanguageCards({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
